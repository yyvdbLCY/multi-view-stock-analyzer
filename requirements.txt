python-telegram-bot>=20.7
google-generativeai>=0.8.0
yfinance>=0.2.40
python-dotenv>=1.0.0
pydantic>=2.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
# httpx is a fastapi dep but pin to avoid surprises
httpx>=0.27.0
pyyaml>=6.0
# yt-dlp still kept as a last-resort fallback if YouTube Data API fails.
# Captions normally require OAuth, so the YouTube API path is the
# primary; this is just an escape hatch for power users.
yt-dlp>=2024.10.7
youtube-transcript-api>=0.6.0

# Optional: only needed if ENABLE_WHISPER_FALLBACK=true
# openai-whisper>=20231117
