python-telegram-bot>=20.7
google-generativeai>=0.8.0
yfinance>=0.2.40
python-dotenv>=1.0.0
pydantic>=2.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
# httpx is a fastapi dep but pin to avoid surprises
httpx>=0.27.0
pyyaml>=6.0
# YouTube transcript fetcher (no API key, no OAuth)
youtube-transcript-api>=0.6.2
# CA bundle for SSL on minimal Python installs (Vercel, GitHub Actions)
certifi>=2024.7.4

# Optional: only needed if ENABLE_WHISPER_FALLBACK=true (audio transcription)
# Requires ffmpeg + ~150MB openai-whisper package. Not used by the main path.
# openai-whisper>=20231117
